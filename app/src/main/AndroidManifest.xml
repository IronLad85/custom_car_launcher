<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- INTERNET: telemetry upload (HTTP POST to the Car Telemetry Server,
         TelemetryLogger). Car data comes from the CAN Sniffer over USB only. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location: GPS fixes from the head unit's receiver attach to telemetry
         rows at recording time. Requested at runtime on launch; the pipeline
         works without it (samples just omit GPS). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Declares compatibility with automotive devices; not required to install elsewhere. -->
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="false" />

    <!-- Package visibility: allows queryIntentActivities(ACTION_MAIN/CATEGORY_LAUNCHER)
         to enumerate all launchable apps on API 30+ without QUERY_ALL_PACKAGES. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarHeadUnit"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Reads the active media notification for the now-playing tile.
             Requires the user to grant "Notification access" in Settings. -->
        <service
            android:name=".data.MediaNotificationService"
            android:exported="false"
            android:label="Media notifications"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:clearTaskOnLaunch="true"
            android:excludeFromRecents="true"
            android:screenOrientation="landscape"
            android:label="@string/app_name"
            android:theme="@style/Theme.CarHeadUnit">

            <!-- HOME intent filter: registers this app as a launcher. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- LAUNCHER filter kept so the app can be opened directly
                 (from another launcher, adb, or settings). -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- USB attach for the CAN Sniffer (ESP32-S3) -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.device_filter"
                android:resource="@xml/usb_device_filter" />
        </activity>
    </application>

</manifest>
